apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: jonas-nginx-demo
  labels:
    app: jonas-nginx-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: jonas-nginx-demo