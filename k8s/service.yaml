apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: thaiha-nginx-demo
  labels:
    app: thaiha-nginx-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: thaiha-nginx-demo
